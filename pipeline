pipeline {
    agent any
    options {
        // Timeout counter starts AFTER agent is allocated 1
        timeout(time: 1, unit: 'SECONDS')
    }
    stages {
        stage('Example') {
            steps {
                echo 'Hello World'111
            }
        }
    }
}
